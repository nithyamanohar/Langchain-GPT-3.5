> Obtain API keys from app.activeloop.ai and https://platform.openai.com and pass as environment variables 
    export OPENAI_API_KEY=<your_api_key>
    export ACTIVELOOP_TOKEN=<your_api_key>

> Change line 21, 22 and 23 with deep lake account name, dataset name and path to code file respectively 

> Uncomment line 93 to create a dataset on the first run and comment it for subsequent runs  

> Lines 61, 62, 63 are prompts I tuned to get the best performance 

> Run file using `streamlit run lamini.py`

> Attaching sample screenshots to the zipfile 